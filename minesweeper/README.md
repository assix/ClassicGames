# Minesweeper (Python/Pygame)

Classic logic puzzle. Features recursive flood-fill, first-click safety, and 'Chording' (double-click to clear neighbors).

This game features custom procedural graphics and requires no external assets.

## 🛠️ Installation & Run

1. **Install Dependencies**
   ```bash
   pip3 install -r requirements.txt
   ```

2. **Run the Game**
   ```bash
   python3 minesweeper.py
   ```
